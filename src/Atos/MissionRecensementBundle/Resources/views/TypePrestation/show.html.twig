{% extends 'AtosMissionRecensementBundle::layout.html.twig' %}

{% block title %}
    Type prestation - {{ entity.nom }}
{% endblock %}

{% block pre_container %}
    <h1>Type prestation</h1>
{% endblock %}

{% block container -%}

    <table class="record_properties table table-hover table-striped table-condensed">
        <tbody>
            <tr>
                <th>Type prestation</th>
                <td>{{ entity.nom }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('typeprestation') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('typeprestation_edit', { 'id': entity.id }) }}">
            Éditer
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
