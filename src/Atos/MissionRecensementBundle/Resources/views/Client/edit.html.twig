{% extends '::base.html.twig' %}

{% block title -%}
    Édition client
{% endblock %}


{% block body -%}
    <h1>Édition de client</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('client') }}">
            Retour à la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
