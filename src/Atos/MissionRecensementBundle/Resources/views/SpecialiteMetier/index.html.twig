{% extends 'AtosMissionRecensementBundle::layout.html.twig' %}

{% block title -%} 
    Liste des spécialités métiers
{% endblock %}

{% block pre_container %}
    <h1>Liste des spécialités métiers</h1>
{% endblock %}

{% block container -%}

    <table class="records_list table table-hover table-striped table-condensed">
        <thead>
            <tr>
                <th>Spécialité métier</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.nom }}</td>
                <td>
                <div>
                    <div>
                        <a href="{{ path('specialitemetier_show', { 'id': entity.id }) }}">{{ icon('eye-open') }} Visualiser</a>
                    </div>
                    <div>
                        <a href="{{ path('specialitemetier_edit', { 'id': entity.id }) }}">{{ icon('pencil') }} Éditer</a>
                    </div>
                </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <div>
        <div>
            <a href="{{ path('specialitemetier_new') }}">
                {{ icon('plus') }} Créer une nouvelle entrée
            </a>
        </div>
    </div>
    {% endblock %}
