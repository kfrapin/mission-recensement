{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SpecialiteMetier Éditer</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('specialitemetier') }}">
            Retour à Liste de lase
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
