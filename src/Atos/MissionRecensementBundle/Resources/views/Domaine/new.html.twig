{% extends 'AtosMissionRecensementBundle::layout.html.twig' %}

{% block body -%}
    <h1>Domaine creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('domaine') }}">
            Retour à la liste
        </a>
    </li>
</ul>
{% endblock %}
